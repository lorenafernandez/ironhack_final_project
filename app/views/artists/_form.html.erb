
	<%= form_for artist do |f| %>

		<div class="my_field">
		    <%= f.label :you_are, "Eres *" %>
			<%= f.select(:you_are, options_for_select(Artist::TYPE_OF_ARTISTS)) %>
		</div>

		<div class="my_field">
		    <%= f.label :type_of_professional, "Tipo de profesional *" %>
			<%= f.select(:type_of_professional, options_for_select(Artist::TYPE_OF_PROFESSIONALS)) %>
		</div>

		<div class="my_field">
		    <%= f.label :type_of_locals, "Lugares en los que sueles exponer *" %>
			<%= f.select(:type_of_locals, options_for_select(Artist::TYPE_OF_LOCALS)) %>
		</div>

		<div class="my_field">
		    <%= f.label :agreements_with_locals, "Te interesaría un local *" %>
			<%= f.select(:agreements_with_locals, options_for_select(Artist::TYPE_OF_AGREEMENTS)) %>
		</div>

		<div class="my_field">
		    <%= f.label :type_of_expositions, "Te gusta que tus exposiciones sean *" %>
			<%= f.select(:type_of_expositions, options_for_select(Artist::TYPE_OF_EXPOSITIONS)) %>
		</div>

      	<div class="btton_register">
      	  	<%= f.submit"Finalizar" %>
      	</div>


	<% end %>